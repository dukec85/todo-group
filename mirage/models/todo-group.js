import { Model } from 'ember-cli-mirage';

export default Model.extend({

});

this.get('/todo-group');
this.post('/todo-group');
this.get('/todo-group/:id');
this.put('/todo-group/:id');
this.patch('/todo-group/:id');
this.del('/todo-group/:id');
