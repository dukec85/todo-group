import { Model } from 'ember-cli-mirage';

export default Model.extend({

});

this.get('/posts');
this.post('/posts');
this.get('/posts/:id');
this.put('/posts/:id');
this.patch('/posts/:id');
this.del('/posts/:id');
